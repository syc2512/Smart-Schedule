# 2026-07-16 工作日志

## 智能课程表 Pro 前端实现
- 依据 `D:/实验室选拔/需求/智能课程表Pro-PRD.md` 与 `D:/实验室选拔/需求/UI设计规范.md` 实现。
- 交付物：`D:\实验室选拔\前端\smart-timetable-pro.html`（单文件、零构建、原生 JS，可直接双击打开）。
- 严格使用 UI 规范的 Design Token：冷杉青绿主色 `#0d9488`、暖琥珀 `#f59e0b`、纸感背景 `#fafaf9`、12 色无障碍课程调色板、4pt 间距、圆角/阴影、`cubic-bezier(.22,1,.36,1)` 动效、`prefers-reduced-motion`。
- 已实现功能：
  - 周视图（7×N 网格、节次纵轴、按类型着色、单双周角标、跨节合并、空闲占位、当前日列高亮、重叠课程分道并排）。
  - 日视图（时间轴、课程卡、空闲块含时长、当前时间红线）。
  - 筛选搜索（课程名/教师 + 上午/下午/晚上），非匹配项淡化。
  - 冲突检测（同星期+节次重叠+单双周交叠）→ 红环高亮 + ⚠角标 + aria-live 横幅。
  - 课程详情抽屉、增/改/删表单（实时校验 + 冲突预警不阻断）、删除二次确认 + 30s 软删除撤销 Toast。
  - localStorage 持久化 + 示例数据（含一处演示冲突）。
- 验证：node `new Function` 语法校验通过；jsdom 冒烟测试跑通周/日视图、筛选、表单、详情全部路径，无运行时错误，错误兜底页未触发。
- 复用经验：对"PRD + UI 规范 md → 单文件 HTML 应用"类任务，优先原生 JS（满足 PRD 首屏<200ms、零控制台错误），而非 React+CDN（Babel 拖慢首屏）。

## 智能课程表 Pro — 审查改进（对标 PRD 全量）
- 用户要求按 5 个关注点（结构规范 / 语义化 / 可访问性 / 元数据 / 冗余）审查并改进 `smart-timetable-pro.html`，范围含 PRD 进阶功能 F-08/F-09/F-10。
- 审查计划写入 `C:\Users\lenovo\.workbuddy\plans\quantum-aurora-lovelace.md`，已批准。
- 改进落地（单文件重写）：
  - Phase0 可达性/语义：日视图卡片改 `<button>`；表单 label `for` 关联；sheet `h3` 加 `id=sheetTitle` + 焦点陷阱/还原；weekday/wt/palette 改 `radiogroup`（含方向键），视图切换/时段筛选改 `aria-pressed` toggle；weeknav 改 `<nav>` 地标；冲突横幅改 `aria-live=polite` 且仅冲突集合变化时播报；`<main>` 动态 aria-label；错误页按钮改 addEventListener。
  - Phase1 产品级：实现单双周"错周隐藏"横幅（AC-03）；日视图整天空闲空状态 + 双 CTA；PWA（manifest.webmanifest + icon.svg + meta description/color-scheme）；IndexedDB 课程持久化 + localStorage 配置，抽 `StorageAdapter` 自动降级；骨架屏加载态；提取 `ROW_H` 常量消除魔法数、`computeConflicts` 单次复用、now-line 局部更新不打断 SR、合并连续空闲占位。
  - Phase2 进阶功能：F-08 AI 一键导入（文本+截图、OpenAI 兼容、本地 Key、预览确认+可疑字段标红、NO_KEY 降级提示）；F-09 空闲规划助手（目标模式、AI/模板兜底）；F-10 .ics 导出（RFC5545、单双周 INTERVAL=2 拆分、范围/课前提醒/备注开关、下载引导）；设置页（学期起始/总周数/AI Key/JSON 备份恢复/.ics 入口）。
- 验证：node --check 语法通过；jsdom 冒烟测试 26 项全过、0 运行时错误；.ics 导出专项测试 10 项全过（结构/单双周拆分/VALARM/UID 正确）。
- 新增配套文件：`manifest.webmanifest`、`icon.svg`（与 html 同目录）。
- 踩坑：周/单双周按钮生成的三元表达式外层括号未闭合导致 `;` 语法错误，且 "active" 分支漏了 `></button>`；已用显式变量重写修正。
