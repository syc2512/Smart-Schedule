# 2026-07-17 工作日志

## 验证 smart-timetable-pro 数据层调用链路
- 文件：`timetable-data.js`（数据）+ `smart-timetable-pro.html`（逻辑 UI）
- 结论：数据层确实被调用。链路 `timetable-data.js → window.TIMETABLE_DATA → var DATA → 业务逻辑` 完整。
- 加载：HTML 第458行 `<script src="timetable-data.js">`；第465行 `var DATA = window.TIMETABLE_DATA`。
- 读路径：`boot()` 调用 `StorageAdapter.loadCourses()`（IndexedDB `getAll()`，失败降级 localStorage `getItem`）；首次无数据则 `seed()` 读 `DATA.seedCourses`（23条）。
- 消费：`DATA.*` 在 HTML 中引用 92 次，覆盖全部 11 个字段（ai/colorPalette/exportOptions/icsDayCodes/layout/planner/seedCourses/storage/timeSlots/weekTypes/weekdays）。
- 风险（已修复）：数据文件缺失时无防御，DATA 为 undefined 会导致全应用崩溃。已在第465行后补守卫：`if (!DATA || typeof DATA !== "object"){...errPage.show(); return;}`。
- 用 Node 脚本（C:/Users/lenovo/AppData/Local/Temp/verify_data.js）做了可运行验证，复跑确认防御检查已生效。

## 真假数据分离（模拟数据外置到 test_data.js）
- 识别：timetable-data.js 中 `seedCourses`（23条示例课程）是「本应由后端 API 返回」的模拟数据；其余字段（timeSlots/colorPalette/layout/ai 等）为前端静态配置，不抽。
- 新建 `test_data.js`：导出 `mockCourses`（字段对齐未来 GET /api/courses），顶部注释标明临时模拟、后端就绪后删除并替换为真实接口。双导出：浏览器全局 `window.STP_MOCK_DATA` + CommonJS `module.exports`（未用 ESM export 因 file:// 下普通脚本不支持 module，保持现有功能）。
- timetable-data.js：`seedCourses` 改为 `(window.STP_MOCK_DATA ? window.STP_MOCK_DATA.mockCourses : [])`。
- HTML：`test_data.js` 在 `timetable-data.js` 之前加载；数据分层注释已更新。
- 验证脚本（C:/Users/lenovo/AppData/Local/Temp/verify_separation.js）：模拟加载顺序，确认 seedCourses 仍 23 条、字段一致、加载顺序正确，功能不变。

## 前端交互分析 + 后端架构选型（聚焦需求）
- 任务：基于 smart-timetable-pro.html 实际代码，分析其 API/表单/渲染交互，给出 2-3 个后端架构选项及适用场景、优缺点、技术栈配合。
- 代码实证：核心功能**全本地**（IndexedDB 主、localStorage 兜底），无自有后端 fetch；唯一网络出口是 `AIAdapter` 浏览器直连 OpenAI 兼容 `/chat/completions`（用户自带 Key，Bearer 直发）。Course 数据契约 9 字段已固化（normalize/stripAll/test_data.js 对齐 GET /api/courses）。表单/导入/导出(.ics/JSON)/冲突检测全在客户端。
- 产物：`backend-architecture-proposal.md` —— 三方案：A 本地优先+轻量AI代理(零账号)；B 容器化 REST(Node+Postgres+Redis)；C Serverless BFF+AI网关(推荐)。含 PG schema、API 契约、增量同步策略、AI网关安全、与前端(原生JS/IndexedDB/PWA/SheetJS)配合说明。
- 关键建议：保留本地优先，StorageAdapter 仅加后台同步钩子；前端<10行改动即可切到服务端AI网关。

## 方案A落地：Python 本地后端 + 前端 API 对接
- 任务：按方案A用 Python 实现本地后端，返回 test_data.js 数据，前端收到才展示，无数据库。
- 产物 `app.py`（零依赖，仅标准库 http.server）：
  - `GET /api/courses` 返回 mockCourses（23门，8字段 name/teacher/location/weekday/startPeriod/endPeriod/weekType/colorIndex），支持可选过滤 `?weekday=&weekType=&name=`。
  - 同时托管前端静态文件（`/`→smart-timetable-pro.html，白名单扩展名 .html/.js/.json/.webmanifest/.svg/.png/.css 等），带 CORS、路径穿越防护、`.py`源码拒绝(404)、文件 mtime 变更自动重读缓存。
  - 解析 test_data.js：regex 定位 `mockCourses=[` + 平衡括号匹配提取 + json.loads（数组部分是合法JSON）。
  - 启动：`python app.py` → http://localhost:8000/ ；用 managed Python 3.13.12 跑通。
- 前端改造 `smart-timetable-pro.html`：
  - 移除 `<script src="test_data.js">`，使后端 API 成为唯一数据源（test_data.js 退为后端数据文件）。
  - `boot()` 改为 `fetch("/api/courses",{cache:"no-store"})` → 成功 `normalize` 后渲染并存 IndexedDB；失败 `state.courses=[]` 渲染空状态（不显示种子数据）。满足"未收到数据不显示"。
  - 数据层注释同步更新。seed() 不再被调用但保留定义。
- 验证：/api/courses 23条字段一致；weekday=1→4条、weekType=odd→3条、name=数据→3条；/app.py→404；首页/timetable-data.js→200。后端当前后台运行(task tqQDTt, port 8000)。
- 注意：后端只读 test_data.js，用户在会话内增删课程不会持久化到后端（刷新恢复 mockCourses），符合"无数据库、数据来自test_data.js"的要求。

## 一键启动脚本 start_server.bat
- 需求：双击即可启动服务器，自动定位当前目录，窗口保持可见看日志，失败时窗口不关并显示错误。
- 实现 `start_server.bat`（ASCII 英文内容，避免中文路径编码乱码）：
  - `cd /d "%~dp0"` 自动定位到 .bat 所在目录（不硬编码中文路径 D:\实验室选拔\Work）。
  - `chcp 65001` 切 UTF-8，保证 app.py 中文日志正常显示。
  - Python 探测链：托管版 `C:\Users\lenovo\.workbuddy\binaries\python\versions\3.13.12\python.exe` → PATH 中 python → py；找不到则报错+pause。
  - `"%PY_EXE%" app.py` 前台运行（窗口常驻显示日志），退出后 `set RC=%errorlevel%` + 显示退出码与排错提示（端口占用/test_data.js 缺失）+ `pause` 保持窗口。
  - app.py 缺失走 `:noapp` 分支报错+pause。
- 验证：端口8000空闲、托管Python存在、app.py就位、python在PATH。沙箱禁止工具内调 cmd.exe，无法运行 .bat 本体，但核心 `python app.py` 已验证可用。
- 已停止之前后台测试用的服务器实例(tqQDTt)，端口留给用户双击启动。

## 大模型解析全链路：客户端→服务端→大模型
- 需求：用户点按钮发文本到服务端，服务端组装Prompt调免费大模型，包装成与 test_data.js 一致的结构返回；客户端等待时循环播放加载动画，收到数据停止并展示；失败/超时停止动画并提示。
- 后端 app.py 新增 `POST /api/ai/parse`（零依赖 urllib）：
  - 读 `{text}` → 用镜像自 timetable-data.js 的 parseText 提示词组装 messages → 调 OpenAI 兼容 `/chat/completions`（默认 SiliconFlow `Qwen/Qwen2.5-7B-Instruct`）→ `extract_json_array` 提取(兼容```json```包裹) → `wrap_courses` 规范化为8字段 → 返回数组。
  - 异常类 `LlmError(code)`：LLM_NOT_CONFIGURED(503)/LLM_TIMEOUT/LLM_HTTP_ERROR/LLM_CONNECT_ERROR/LLM_BAD_RESPONSE(均502)，返回 `{error,code}`。
  - 配置 `llm_config.json`（api_key/base_url/model/timeout/mock）+ 环境变量覆盖；`mock:true` 跳过真实API返回固定数组用于链路测试。banner 打印 LLM 状态。
  - CORS 放开 POST；`do_POST`+`_read_json_body`+`_handle_ai_parse`。
- 前端 smart-timetable-pro.html：
  - 新增 `serverParse(text)`(fetch POST /api/ai/parse) + `showAiLoading/hideAiLoading`(循环 CSS spinner `.ai-spinner` @aiSpin infinite)。
  - 解析按钮：文本/xlsx 走 serverParse；图片/PDF 仍走浏览器 AIAdapter。请求中 showAiLoading 循环动画，完成 hideAiLoading；失败按 NO_KEY/Failed to fetch/其他 分支 toast，动画停止。
- 验证：无Key→503 LLM_NOT_CONFIGIFIED 明确错误；mock模式→200 返回数组，8字段与 test_data.js 完全一致，weekday/period/weekType/colorIndex 约束全过；wrap_courses 对代码块包裹+越界值(weekday9→7,period15→12,end<start对齐,weekType非法→all,colorIndex99→11)+缺名丢弃均正确。
- 默认 mock=false(真实免费大模型，需用户填Key)；用户可设 mock=true 立即测全链路。已停测试服务器(eJAQjT)，端口空闲。

## 多模态解析统一收归服务端（图片/PDF）
- 需求：图片/PDF 解析也走服务端，客户端不再维护解析逻辑；单一 llm_config.json 含多模态模型定义；服务端按文件类型路由；向后兼容文本。
- llm_config.json 新增 `models`：text(Qwen2.5-7B-Instruct, inputs:text) + vision(Qwen2-VL-72B-Instruct, inputs:image,pdf)，各自 base_url/model/inputs；保留共享 api_key/timeout/mock 与扁平 base_url/model 回退。
- app.py：
  - `load_llm_config` 解析 models；`route_model(cfg,input_type)` 按 inputs 路由，text 无模型时回退扁平配置，image/pdf 无模型返回 None。
  - `call_llm_parse(input_type,text,data,cfg)`：mock 优先→检查 key→路由→text 用纯文本消息，image/pdf 用 `[{type:image_url,image_url:{url:data}}]` 多模态消息(OpenAI兼容)→`_do_completion` 统一 HTTP。
  - `_handle_ai_parse` 接收 `{type,text,data}`，type 缺省 text(向后兼容旧 {text})；非法 type→400 BAD_TYPE；缺 data→400 EMPTY_DATA；无对应模型→503 LLM_NO_MODEL_FOR_TYPE。
  - banner 打印 text/image/pdf 三类路由结果。
- 前端 smart-timetable-pro.html：
  - `serverParse(payload)` 改接收对象；新增 `arrayBufferToBase64`(分块)。
  - 解析按钮 image/pdf/xlsx/text 全部走 serverParse({type,data}/{type,text})，移除 AIAdapter 在解析流程的使用(AIAdapter 保留给规划助手)。
  - 导入提示文案改为"由本地后端调用大模型解析，按文件类型自动路由"。
- 验证(mock=true)：文本(无type)向后兼容200、文本(type)200、image200(mock名含图片)、pdf200(mock名含PDF)，均8字段一致；非法type→400 BAD_TYPE；缺data→400 EMPTY_DATA。route_model 单测：完整配置 image/pdf→vision，无vision配置→None。call_llm_parse 单测：有key无vision→image抛 LLM_NO_MODEL_FOR_TYPE(不触网)，text 路由回退正常。
- PDF 经 image_url 以 data:application/pdf;base64 透传，是否支持取决于模型(已注释说明)。默认 mock=false，端口空闲。

