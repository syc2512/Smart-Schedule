# 智能课程表 Pro — 用户配置管理系统 依赖
# ============================================================
# 安装: pip install -r requirements.txt
# 推荐使用隔离 venv，避免污染系统 Python

# 大模型 SDK (可选，未安装时自动回退到标准库 urllib)
openai>=1.0.0

# 用户配置管理系统（必装，启用 MySQL 持久化）
PyMySQL>=1.1.0           # 纯 Python MySQL 驱动，Windows 上易安装
cryptography>=42.0.0     # Fernet 对称加密 (AES-128-CBC + HMAC-SHA256)
