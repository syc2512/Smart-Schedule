### 一、

这个HTML文件中包含大量硬编码的数据，需要将数据与逻辑进行分离。请将所有写死的数据提取到一个独立的JSON格式数据文件中，使HTML文件通过读取该JSON文件来获取所需数据，实现数据与展示逻辑的解耦。确保JSON文件结构清晰、字段命名规范，并保持原有数据的完整性和正确性。

### 二、
现在逻辑与数据确实进行了分离，但请确认程序是否读取了数据：验证数据层是否被实际调用，包括检查数据源（如数据库、API、文件）的连接是否建立、查询或读取操作是否执行、返回的数据是否被业务逻辑层正确消费。


### 三、
在当前项目的.js文件中，存在一些本应由后端API返回但被硬编码在前端的数据。请完成以下操作：1. 识别并提取这些模拟数据，将其集中存放到一个新建的test_data.js文件中，以模块化方式导出（如使用export或module.exports）。2. 在原.js文件中，将原本直接引用硬编码数据的地方，改为从test_data.js导入，保持前端逻辑不变。3. 确保数据结构命名清晰，便于后续替换为真实后端接口返回值。4. 在test_data.js文件顶部添加注释，标明此文件为临时模拟数据，后端接口开发完成后应删除该文件并将引用替换为真实API调用。目标是在不改变现有功能的前提下，实现真假数据的清晰分离，为后续接入后端接口做好准备。



